# N100 Midday Web-View Perf Check

- run_window: `2026-06-04 12:02:12` to `2026-06-04 12:06:11` KST
- scope: read-only production `web-view` midday status and performance check
- verification_commands:
  - `Invoke-WebRequest <LOCAL_WEB_VIEW_URL>/health`
  - `Invoke-WebRequest <LOCAL_WEB_VIEW_URL>/`
  - `Invoke-WebRequest <LOCAL_WEB_VIEW_URL>/api/daily/latest`
  - `Invoke-WebRequest <LOCAL_WEB_VIEW_URL>/api/market`
  - `Invoke-WebRequest <LOCAL_WEB_VIEW_URL>/api/candidate-evidence`
  - `.venv\Scripts\python.exe -m stock_monitor web-view-browser-smoke --date latest --json`
  - `.venv\Scripts\python.exe -m stock_monitor api-perf-summary --json`

## Live /health

- local live target: `<LOCAL_WEB_VIEW_URL>`
- `GET /health` -> `200`, `text/plain`, body `ok`
- live loopback health was available during the midday check

## Access Gate Result

- `GET /` -> `401`
- `GET /api/daily/latest` -> `401`
- `GET /api/market` -> `401`
- `GET /api/candidate-evidence` -> `401`
- interpretation: `access_gate_401` confirmed on protected live routes; this is not treated as a route-contract failure
- no access-code, `.env`, secret, or bypass value was read or supplied

## Browser Smoke Result

- command date resolved to `business_date=2026-06-04`
- `read_only=true`
- `access_code_mode=<LOCAL_SMOKE_ACCESS_MODE>`
- `issue_count=0`
- required API checks from the smoke server passed:
  - `GET /api/daily/2026-06-04` -> `200`
  - `GET /api/daily/{date}?intraday_market_top=1` -> `200`
  - `GET /api/candidate-evidence` -> `200`
  - `GET /api/daily/{date}/stocks/{stock_code}` -> `200`
  - `POST /api/daily/2026-06-04` -> `405`
  - `GET /api/status` -> `404`

## Intraday Display Observation

- all four tested viewports (`desktop`, `tablet`, `large_mobile`, `mobile`) reported:
  - tab count `5`, order `main/watch/stock/market/rotation`
  - stock search visible
  - `장중 거래대금 확인` button visible
  - `Naver 장중 참고` overlap panel present
  - watch/stock/market/rotation tabs clickable
  - horizontal overflow `0px`
- `watch_observation_summary_visible=true` on every viewport
- `observation_summary_main_visible=false` and `intraday_overlap_initial_visible=false` were consistent across all viewports and did not produce smoke issues

## API Perf Summary

- source: `logs/api_perf.log`
- summary run observed `record_count=565`, `endpoint_count=79`
- live alias `/api/daily/latest` was not present in stored perf history; stored logs track dated daily routes instead
- stored-data behavior for the related daily path on `2026-06-04`:
  - `/api/daily/2026-06-04`: `count=6`, `cache_hits=3`, `cache_misses=3`, `p50_total_ms=1.42`, `p95_total_ms=9551.905`, `avg_db_ms=2656.209`, `avg_build_ms=3465.168`
  - `/api/daily/2026-06-04?intraday_market_top=1&market_top_limit=100&market_top_page_size=20`: `count=2`, `cache_hits=0`, `cache_misses=2`, `p50_total_ms=2391.269`, `p95_total_ms=10352.149`, `avg_db_ms=2469.0`, `avg_build_ms=6368.725`
- stored-data behavior for requested supporting routes:
  - `/api/market`: `count=3`, `cache_hits=1`, `cache_misses=2`, `p50_total_ms=419.381`, `p95_total_ms=454.704`, `avg_db_ms=259.168`, `avg_build_ms=290.907`
  - `/api/candidate-evidence?date=2026-06-04&limit=20`: `count=6`, `cache_hits=3`, `cache_misses=3`, `p50_total_ms=1.603`, `p95_total_ms=822.753`, `avg_db_ms=235.99`, `avg_build_ms=358.391`
- related logged stored-data paths remain materially faster than the cold daily build path:
  - `/api/flow-trend?date=2026-06-02&limit=5`: `p50_total_ms=0.389`, `p95_total_ms=471.534`
  - `/api/etf-trend?date=2026-06-02&limit=5`: `p50_total_ms=0.644`, `p95_total_ms=564.054`
  - `/api/archive?limit=120`: `p50_total_ms=22.794`, `p95_total_ms=136.249`
- inference: the midday performance pressure is concentrated in cold or partially cold stored-data daily payload construction, especially the intraday market-top variant; auxiliary market/candidate/trend routes are materially lighter

## Blockers / Hold Items

- hold: live protected routes are gate-blocked by design, so contract verification for those live endpoints depends on the smoke server path rather than the gated production loopback path
- hold: no direct `/api/daily/latest` perf-history row exists in `api_perf.log`; only the dated daily route is available for stored-data performance interpretation
- blocker: none for this read-only midday observation pass

## Read-Only Boundary Confirmation

- did not read `.env`, secrets, cookies, or access-code values
- did not write to the production DB
- did not call Toss APIs
- did not run live backfill
- did not modify scheduler, Telegram, `admin-gui`, `web-view` process, or network bindings
- wrote only this observation markdown file
