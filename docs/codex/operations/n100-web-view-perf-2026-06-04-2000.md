# N100 web-view perf check - 2026-06-04 20:00 KST

- run_time: `2026-06-04 20:05:34 +09:00`
- scope: read-only evening/tunnel-window production web-view status and performance check
- boundary_confirmation: confirmed read-only only; no `.env` read, no secret/access-code/private URL/token read, no DB write, no Toss API call, no scheduler/Telegram/admin-gui/web-view connection change, no live backfill

## Commands run

```powershell
curl.exe -s -S -D - <LOCAL_WEB_VIEW_URL>/health
.\.venv\Scripts\python.exe -m stock_monitor web-view-browser-smoke --date latest --json
.\.venv\Scripts\python.exe -m stock_monitor api-perf-summary --json
curl.exe -s -o NUL -w "%{http_code}" <LOCAL_WEB_VIEW_URL>/
curl.exe -s -o NUL -w "%{http_code}" "<LOCAL_WEB_VIEW_URL>/api/archive?limit=1"
curl.exe -s -o NUL -w "%{http_code}" "<LOCAL_WEB_VIEW_URL>/api/daily/2026-06-04"
Get-Process cloudflared -ErrorAction SilentlyContinue | Select-Object ProcessName,Id,StartTime | ConvertTo-Json -Compress
```

## Live /health

- local_live_health_url: `<LOCAL_WEB_VIEW_URL>/health`
- result: `200 OK`
- body: `ok`
- assessment: local live web-view process responded on the expected loopback target

## Access gate result

- root_get_status: `401`
- archive_get_status: `401`
- daily_get_status: `401`
- access_gate_result: `access_gate_401`
- assessment: unauthenticated protected live routes are access-gated as expected; this is not treated as a route-contract failure

## Smoke result

- command: `.\.venv\Scripts\python.exe -m stock_monitor web-view-browser-smoke --date latest --json`
- result: `pass`
- read_only: `true`
- access_code_mode: `<LOCAL_SMOKE_ACCESS_MODE>`
- issue_count: `0`
- business_date: `2026-06-04`
- route_contract:
  - `GET /api/daily/2026-06-04` -> `200`
  - `GET /api/daily/{date}?intraday_market_top=1` -> `200`
  - `GET /api/candidate-evidence` -> `200`
  - `GET /api/daily/{date}/stocks/{stock_code}` -> `200`
  - `POST /api/daily/2026-06-04` -> `405`
  - `GET /api/status` -> `404`
- mobile_layout:
  - desktop/tablet/large_mobile/mobile all reported `horizontal_overflow_px=0`
  - tab order matched `main/watch/stock/market/rotation`
  - search input present
  - representative non-main panels clickable in every viewport

## API perf summary

- source: `logs/api_perf.log`
- records: `625`
- endpoints: `79`
- evening stored-data browsing paths observed:
  - `/api/archive?limit=120`: `93` hits, `p50 2.151 ms`, `p95 183.636 ms`, `p99 2550.958 ms`
  - `/api/daily/2026-06-04`: `16` hits, `11` cache hits, `p50 0.802 ms`, `p95 9551.905 ms`, `max 9551.905 ms`
  - `/api/candidate-evidence?date=2026-06-04&limit=20`: `14` hits, `9` cache hits, `p50 0.652 ms`, `p95 1424.722 ms`
  - `/api/flow-trend?date=2026-06-04&limit=5`: `13` hits, `10` cache hits, `p50 0.249 ms`, `p95 974.773 ms`
  - `/api/etf-trend?date=2026-06-04&limit=5`: `12` hits, `9` cache hits, `p50 0.342 ms`, `p95 1912.156 ms`
  - `/api/rotation-overlay?date=2026-06-04&limit=5`: `13` hits, `10` cache hits, `p50 0.255 ms`, `p95 66.217 ms`
  - `/api/daily/2026-06-04/stocks/039200`: `3` hits, `p50 1252.339 ms`, `max 1656.826 ms`
- notable slower stored-data paths in the recent history window:
  - `/api/daily/*?intraday_market_top=1&market_top_limit=100&market_top_page_size=20` showed the heaviest latency, with worst observed `p95 14360.260 ms`
  - uncached `/api/daily/{date}` builds remain the main evening bottleneck; cached rereads are near-instant at p50 but cold builds still spike to multi-second latency
  - archive, rotation-overlay, observation-backtest, flow-trend, and most ETF-trend requests stayed materially lighter than daily detail builds

## Local / tunnel comparison

- local_availability: `available`
- tunnel_client_process: `cloudflared` process observed locally
- tunnel_check: `held_for_secret_boundary`
- comparison_availability: `partial`
- assessment: local loopback target is healthy and gated; external provider/tunnel availability was not verified because that would require private origin context that this run did not read

## Blockers / hold items

- external tunnel/provider URL status remains unverified in this run by design
- `Test-NetConnection <LOCAL_WEB_VIEW_HOST> -Port <LOCAL_WEB_VIEW_PORT>` timed out during the session, but direct HTTP `/health` succeeded and is treated as the stronger live signal
- performance history is log-derived and not limited to this exact minute; it summarizes the stored-data browsing history currently available in `api_perf.log`

## Outcome

- local live `web-view` is up on `<LOCAL_WEB_VIEW_HOSTPORT>`
- live access gate is active on protected routes
- local browser smoke passed with zero issues under `<LOCAL_SMOKE_ACCESS_MODE>`
- stored-data browsing remains usable, but cold `daily` builds, especially with `intraday_market_top=1`, are the main latency hotspot for evening review
