# N100 Post-Close Web-View Perf Check

- run_window: `2026-06-04 16:11:30` to `2026-06-04 16:15:22` KST
- run_time: `3m 51s`
- scope: read-only production `web-view` post-close status and performance check
- verification_commands:
  - `Invoke-WebRequest <LOCAL_WEB_VIEW_URL>/health`
  - `Invoke-WebRequest <LOCAL_WEB_VIEW_URL>/`
  - `Invoke-WebRequest <LOCAL_WEB_VIEW_URL>/api/status`
  - `Invoke-WebRequest <LOCAL_WEB_VIEW_URL>/api/daily/latest`
  - `Invoke-WebRequest <LOCAL_WEB_VIEW_URL>/api/market`
  - `Invoke-WebRequest <LOCAL_WEB_VIEW_URL>/api/flow-trend`
  - `Invoke-WebRequest <LOCAL_WEB_VIEW_URL>/api/etf-trend`
  - `.venv\Scripts\python.exe -m stock_monitor web-view-browser-smoke --date latest --json`
  - `.venv\Scripts\python.exe -m stock_monitor api-perf-summary --json`

## Live /health Result

- local live target: `<LOCAL_WEB_VIEW_URL>`
- `GET /health` -> `200`, `text/plain; charset=utf-8`, body `ok`
- live loopback health was available during the post-close check

## Access-Gate Result

- `GET /` -> `401`
- `GET /api/status` -> `401`
- `GET /api/daily/latest` -> `401`
- `GET /api/market` -> `401`
- `GET /api/flow-trend` -> `401`
- `GET /api/etf-trend` -> `401`
- interpretation: `access_gate_401` confirmed on protected live routes; this is not treated as a route-contract failure
- no access-code, `.env`, secret, or bypass value was read or supplied

## Smoke Result

- `surface=web-view-browser-smoke`
- `business_date=2026-06-04`
- `read_only=true`
- `access_code_mode=<LOCAL_SMOKE_ACCESS_MODE>`
- `issue_count=0`
- required API checks from the smoke server passed:
  - `GET /api/daily/2026-06-04` -> `200`
  - `GET /api/daily/{date}?intraday_market_top=1` -> `200`
  - `GET /api/candidate-evidence` -> `200`
  - `GET /api/daily/{date}/stocks/{stock_code}` -> `200`
  - `POST /api/daily/2026-06-04` -> `405`
  - `GET /api/status` -> `404`

## Mobile Layout / Route Contract Observation

- tested viewports: `desktop`, `tablet`, `large_mobile`, `mobile`
- all viewports reported tab count `5` with order `main` / `watch` / `stock` / `market` / `rotation`
- stock search, intraday button, intraday overlap panel, and candidate panel were present on all viewports
- watch/stock/market/rotation tabs were clickable on all viewports
- horizontal overflow was `0px` on all viewports
- `watch_observation_summary_visible=true` on all viewports
- `observation_summary_main_visible=false` and `intraday_overlap_initial_visible=false` were consistent and did not produce smoke issues

## API Perf Summary

- source: `logs/api_perf.log`
- summary run observed `record_count=595`, `endpoint_count=79`
- live alias `/api/daily/latest` was not present in stored perf history; stored logs track dated daily routes instead
- relevant stored-data endpoints for `2026-06-04`:
  - `/api/daily/2026-06-04`: `count=11`, `cache_hits=7`, `cache_misses=4`, `p50_total_ms=0.903`, `p95_total_ms=9551.905`, `avg_db_ms=1596.765`, `avg_build_ms=2167.633`
  - `/api/daily/2026-06-04?intraday_market_top=1&market_top_limit=100&market_top_page_size=20`: `count=3`, `cache_hits=0`, `cache_misses=3`, `p50_total_ms=2790.285`, `p95_total_ms=10352.149`, `avg_db_ms=1990.964`, `avg_build_ms=5173.737`
  - `/api/candidate-evidence?date=2026-06-04&limit=20`: `count=10`, `cache_hits=6`, `cache_misses=4`, `p50_total_ms=1.603`, `p95_total_ms=1424.722`, `avg_db_ms=240.979`, `avg_build_ms=357.372`
  - `/api/market`: `count=3`, `cache_hits=1`, `cache_misses=2`, `p50_total_ms=419.381`, `p95_total_ms=454.704`, `avg_db_ms=259.168`, `avg_build_ms=290.907`
  - `/api/flow-trend?date=2026-06-04&limit=5`: `count=9`, `cache_hits=7`, `cache_misses=2`, `p50_total_ms=0.307`, `p95_total_ms=974.773`, `avg_db_ms=103.871`, `avg_build_ms=184.407`
  - `/api/etf-trend?date=2026-06-04&limit=5`: `count=8`, `cache_hits=6`, `cache_misses=2`, `p50_total_ms=0.342`, `p95_total_ms=1912.156`, `avg_db_ms=266.118`, `avg_build_ms=319.187`

## Post-Close Stored-Data / Status Observation

- live loopback service was healthy, and protected live routes stayed access-gated rather than publicly exposed
- the smoke server confirmed the public contract remains GET-only for user data and still blocks `POST /api/daily/2026-06-04` with `405`
- post-close stored-data pressure remains concentrated in the daily payload build path, especially the `intraday_market_top` variant where all observed requests were cache misses and `p95_total_ms` exceeded `10s`
- the lighter supporting paths (`/api/market`, `/api/flow-trend`, `/api/etf-trend`) stayed materially below the daily build path, so the main scheduler-overlap risk remains daily page assembly rather than auxiliary summary endpoints

## Blockers / Hold Items

- hold: protected live routes are gate-blocked by design, so live route-contract verification for those paths depends on the smoke server rather than direct loopback responses
- hold: no direct `/api/daily/latest` perf-history row exists in `api_perf.log`; dated daily routes remain the available stored-data proxy
- blocker: none for this read-only post-close observation pass

## Read-Only Boundary Confirmation

- did not read `.env`, secrets, cookies, or access-code values
- did not write to the production DB
- did not call real Toss APIs
- did not run live backfill
- did not modify scheduler, Telegram, `admin-gui`, `web-view` process, or network bindings
- wrote only this observation markdown file
