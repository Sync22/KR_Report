# Operator Memos

> 26.05.10 재평가 기준: 기존 표기를 신뢰하지 않고 구현 증거, DB 상태, canonical docs, 에이전트 리뷰 기준으로 다시 분류했다. `[O]`는 현재 V1 목적 충족, `[△]`는 기반/계획/부분 구현, `[ ]`는 아직 착수 전으로 본다.
> 26.05.14 원칙 보정: `[O]`는 구현 부품이 있다는 뜻이 아니라 메모의 작성 의도에 맞는 사용자-facing 결과가 나온 경우에만 준다. DB/API/테스트/표시 기반만 있으면 `기반 완료` 또는 `[△]`로 둔다. 앞으로는 완벽한 모델보다 러프한 실사용 브리핑을 우선하고, 매일 수집 결과를 보며 고도화 가능 축을 판단한다.

- [△] 26.04.30 08:28 | 전일자 리포트 기준 섹터 분류 및 그래프로 표현해서 어느섹터의 이야기가 활발히 나왔는지
  - 재평가: 섹터/업종 저장, 롤업, admin/web-view 표시 기반은 있으나 메모 원문처럼 그래프 표현까지 닫힌 상태는 아님. 현재 분류는 KRX 정식 업종이 아니라 taxonomy/cache 기반 참고값이므로 V1 부분 구현으로 둔다.
- [△] 26.05.04 12:30 | 대충 시장의 오늘 분위기를 요약할 수 있는 기능 생각해보기
  - 기반 완료: 최신 요약일 기준 리포트 수, 종목 수, 활성 섹터 수, 최다 섹터, 다건 종목 수, 매수 의견 수를 `admin-gui` 시장 분위기 카드에 표시. 점수/예측형 산식은 데이터 축적 후 후속으로 분리.
  - 26.05.14 보정: 작성 의도는 admin 카드보다 Telegram/web-view에서 바로 읽히는 `오늘의 시장 분위기` 브리핑에 가까움. 기반은 있으나 16:00 전후 장마감 브리핑 포맷/테스트 발송이 필요하므로 후속 고도화 항목으로 분리한다.
- [O] 26.05.05 19:17 | https://m.stock.naver.com/domestic/home/theme/505 해당 링크 내용으로 테마도 표기되도록 조치해보는건 어떨지
  - 재평가: 메모에 적힌 `505` 단건 테마 수집/저장/표시 경로는 구현됨. 운영 DB 기준 `stock_theme_memberships=67`, theme code `505` 확인. 전체 테마 자동 탐색이나 정기 갱신은 후속 항목으로 분리.
- [O] 26.05.05 19:17 | 전일자 리포트의 경우 단건인 경우 우선적으로 제외처리
  - 완료: 전일 요약 발송 기본 필터를 2건 이상으로 강화. 수집/저장은 유지하고 발송에서만 제외.
- [O] 26.05.05 19:17 | 프로그램 자체로 스케쥴을 실행하거나 응답대기 같이 제어할 수 있는 관리 프로그램 기획
  - 완료: `admin-gui` 1차 구현. 스케줄러 상태 확인, Notify/Poll/TelegramCommands 즉시 실행, 작업 활성화/비활성화, 실행 제외 달력 관리 가능. 좁은 화면/스크롤 가로 overflow 렌더 깨짐도 보정.
- [△] 26.05.07 13:53 | 미니피시로 옮기면서 외부에서 관리페이지 접속 구상해보기
  - 재평가: 실제 미니PC 이전, Cloudflare/Tailscale 연결, access gate 구현은 아직 없음. 다만 N100 이전 후 외부 접속 방향과 `admin-gui` 비공개, 친구용 `web-view`만 공유 후보라는 경계는 문서에 반영됨.
  - 추가 검토: 외부 접속 후보는 우선 Tailscale(본인 원격관리)과 Cloudflare Tunnel(친구용 read-only `web-view` 공유) 두 가지로 제한. 직접 포트포워딩은 기본 후보에서 제외.
  - 26.05.09 추가 결정: Docker는 현재 Windows N100 운영에는 도입하지 않음. Cloudflare Tunnel은 친구 공유용 `web-view` 로컬 포트만 연결하고, `admin-gui`, `.env`, DB, Telegram, scheduler/control API는 노출하지 않음.
- [△] 26.05.07 13:54 | 국장 쪽은 테마로 이제 묶을 수 있을 듯하니 해당 부분 반영하여 조정
  - 재평가: `refresh-theme`, `refresh-themes --enabled`, `category_master`, `category_membership_snapshots`, dated category snapshot, admin/web-view 표시 기반은 구현됨. 다만 현재 운영 DB의 theme catalog는 `505` 1개 중심이고, 과거 날짜는 fallback이 남아 있어 국장 전반을 안정적으로 테마로 묶는 단계는 아직 부분 구현이다.
- [△] 26.05.07 13:56 | 업로드해준 순환매매 기준으로 수급 추적 해보기
  - 재평가: KRX 수급 source study, 스키마, 샘플 검증, 수동 import, 사용자용 `web-view` read-only 표시는 구현됨. 자동 보강은 당일 리포트 언급 종목의 `[12009]` 최근 31일 경로로만 허용되며, broad scheduled ingest와 전체 종목 자동 추적은 여전히 금지이므로 V1 기반 완료가 아니라 부분 구현으로 둔다.
  - 26.05.09 추가 확인: KRX Data Marketplace `기본 통계 > 주식 > 거래실적`에서 `[12009] 투자자별 거래실적(개별종목)`, `[12008] 투자자별 거래실적`, `[12010] 투자자별 순매수상위종목` 화면을 확인. `[12009]`를 리포트 종목별 수급 P0 검증 대상으로 승격하고, `[12008]`/`[12010]`은 시장 배경/랭킹 참고값으로 분리.
  - 26.05.09 추가 반영: `[12009]` 기간합계 후보 BLD는 `MDCSTAT02301`로 문서화. 단, 6자리 종목코드가 아니라 `isuCd` 매핑이 필요할 수 있어 dry-run에서 매핑과 단위를 확인하기 전까지 DB 저장/스케줄 수집은 보류.
  - 26.05.09 세션 운영 메모: KRX Data Marketplace 로그인 세션은 약 30분으로 보고, 향후 수급 배치 5분 전 Telegram으로 로그인 요청 알림을 보내는 reminder-only 작업을 우선 고려. 기본은 사용자가 Chrome에서 로그인해둔 세션을 활용하는 방식이며, `LOGOUT`이면 DB 저장 없이 skip/notify.
  - 26.05.09 과거 수급 범위 판단: 가격/거래량 KRX Open API snapshot은 3개월 분석/6개월 보관으로 유지. 투자자별 수급 `[12009]`은 먼저 주도주/관찰 후보 종목-일자만 대상으로 하고, 안정화 전에는 전체 종목/전체 리포트 종목 crawling 금지. `[12008]` 시장 배경과 `[12010]` 순매수 상위는 3개월 범위까지 확장 가능하지만 live dry-run과 단위 검증 후 진행.
  - 26.05.09 P1 반영: 후보 preview, 후보 기반 capture-set scaffold, 캡처 체크리스트, `[12009]` live 화면 검증, 응답 정규화 parser, schema v4 수급 테이블, saved sample 정규화 경로, 명시적 단위 보존 옵션, sample manifest, manifest scaffold, capture-set scaffold, normalized validation artifact, strict sample 품질 검증, sample coverage 상태 점검과 다음 캡처 파일 표시, batch sample 검증과 normalized artifact 일괄 생성, DB import preview, guarded manual sample import, expected investor coverage, capture runbook/template, `db-verify` 수급 품질 게이트, schema 계약 문서, 사용자용 `web-view` 읽기 전용 수급 참고 표시까지 반영.
  - 26.05.10 추가 반영: 로그인된 KRX Data Marketplace visible grid 기준으로 `2026-05-08` `[12009]` 5종목, `[12008]` 시장, `[12010]` 외국인 순매수상위 샘플을 캡처하고 strict 검증 후 수동 import 완료. `krx-flow-raw-sample-scaffold`로 `data\krx_samples_raw` manifest 7개를 준비했고, `krx-flow-compare-samples`로 raw network response 샘플과 visible-grid 샘플의 parity를 확인할 수 있게 했으며, scheduled ingest는 이 비교 통과 전까지 미연결.
  - 26.05.14 보정: 현재 구현은 수급 기반/표시 경로가 중심이고, 메모 의도였던 순환매 이미지 기준 추적과는 아직 괴리가 큼. 순환매는 `이미지 문구/업종 alias -> 리포트 집중 -> 수급/거래대금 -> ETF/종목 후보` 흐름으로 다시 설계해야 한다.
- [O] 26.05.07 13:56 | Etf 종목에 대한 정보를 수집방법 강구 및 디스플레이할 정보 추려내기
  - 완료: KRX ETF snapshot 저장/조회, `GET /api/etf-trend`, 사용자용 `web-view` 읽기 전용 ETF 흐름 표시 기반까지 반영. ETF 해석/비중/구성종목 심화는 별도 고도화로 분리.
- [O] 26.05.09 | 리포트 과거분보다 종목 과거 가격/거래대금 1개월 데이터를 우선 쌓아 추세와 흐름 참고값으로 쓰기
  - 재평가: 과거 리포트 backfill보다 KRX 주식/ETF/지수 일별 snapshot을 우선한다는 기준을 고정했고, 운영 DB 조회 기준 `2026-01-02`부터 `2026-05-08`까지 85개 영업일 snapshot을 확인. 2025년 12월 이전 확장은 현재 목표 밖으로 둔다.
- [O] 26.05.07 13:56 | 관리페이지, 외부공유용 페이지 구분하기
  - 완료: 제어 가능한 `admin-gui`와 읽기 전용 `web-view`를 분리. 공유용 화면에는 스케줄 실행/비활성화/종료 같은 제어 버튼을 두지 않음.
  - 26.05.09 추가 결정: `web-view`는 GET-only 1차 구현 완료 상태로 보고, 남은 작업은 V1 마감 품질(선택 상태, missing-state, archive/mobile polish, public-safe 테스트)로 관리.
- [O] 26.05.10 | 순환매 SVG 오버레이 1차 구현
  - 계획 반영: `example/Cycle.jpg` 위에 SVG overlay layer를 얹고, `data/rotation_overlay_coordinates.json` 수동 좌표맵으로 업종/테마 강조 원을 표시하는 1차 구현 완료. 다음 고도화는 관리자 보정 화면이며, 자동 강조는 리포트 수, 거래대금, 수급 흐름을 분리 증거로 쌓은 뒤 진행.
  - 26.05.14 보정: 1차 overlay 부품은 완료지만, 첨부 이미지에서 기대한 `오늘 어느 영역이 강한지` 경험은 아직 아님. 자동 강조/근거 연결은 `[△]` 고도화로 본다.
- [△] 26.05.10 | 순환매 참고 이미지 내 문구 기준으로 업종/종목 매핑 검토
  - 26.05.15 반영: `data/rotation_image_aliases.json`을 `GET /api/rotation-overlay`에 연결해 이미지 문구 라벨과 현재 업종명/좌표 키를 분리했다. 같은 날짜 리포트/KRX 기반 `candidate_stocks`와 operator-managed `candidate_etfs` preview도 연결했다. 다만 실제 이미지 문구 전체 검수, alias 확대, 관리자 보정 화면은 아직 남아 있어 작성 의도 달성은 진행 중으로 둔다.
  - 재평가: 아직 alias table, OCR 후보, 이미지 문구와 현재 업종/테마/종목 데이터의 매핑 검토 결과가 없음. 내일 할 일로 유지하며, 1차는 OCR 자동화보다 수동 alias table과 좌표맵 보정 기준을 먼저 잡는다.
- [ ] 26.05.08 00:00 | 미국장도 KRX처럼 API 형태로 제공하는 공식/준공식 데이터 소스가 있는지 조사해보기
  - 메모: 미니 PC 이전 후 운영 기반이 안정되면 국내장 외에 미국장까지 시장 범위를 넓힐 수 있는지 검토. 우선 조사 대상은 시세, 거래량/거래대금, ETF, 섹터/산업, 수급 또는 기관/내부자/공시성 데이터 제공 여부. 구현은 아직 보류하고 API 후보, 비용, 인증키, rate limit, 재배포/공유 제한부터 확인.
- [ ] 26.05.11 12:01 | 리포트와 수급을 통해 다음날 기준 2종목 정도를 추천하고자 한다면 어떤식의 가중치가 필요한지
  - 선행조건: 현재는 `추천`이 아니라 `관찰 후보` 기준부터 고정해야 한다. 최소 입력값은 리포트 강도(건수/목표가/의견), 가격·거래량·거래대금 흐름, 종목별 수급, 시장/업종 배경, 제외조건(목표가 없음, 의견 없음, 단건, 유동성 부족)이다.
  - 우선 구현 후보: 점수 확정 전 `candidate_evidence` 형태의 read-only 근거 표를 만들고, 각 종목이 왜 후보로 보였는지 리포트/수급/거래대금/업종 근거를 분리 표시한다. 매수 추천 문구는 금지한다.
  - 26.05.14 보정: `추천`은 계속 금지하되 `눈에 띄는 종목`, `확인 후보`, `수급 참고`는 적극적으로 노출한다. 후보를 보기 위해 과한 백테스트/검증 과정을 먼저 읽게 하는 구조는 줄인다.
- [△] 26.05.11 12:02 | 순환매 기준 표기가 완성된 이후 해당 순환매쪽 ETF와 종목늘 각 1개씩 뽑아낼 수 있는지
  - 선행조건: 순환매 이미지 문구와 현재 업종/테마/종목 데이터의 alias table, 업종 좌표맵, 업종별 대표 ETF 후보, 업종별 대표 종목 후보, 선택일 기준 수급/거래대금/리포트 근거가 필요하다.
  - 우선 구현 후보: `순환매 참고`에서 자동 추천이 아니라 `업종별 대표 ETF/종목 후보`를 근거와 함께 보여주는 read-only preview부터 만든다. ETF/종목 1개 최종 선정은 데이터 축적과 검증 후 별도 단계로 둔다.
  - 26.05.15 일부 반영: `candidate_stocks` read-only preview를 `GET /api/rotation-overlay`와 사용자 화면에 추가했다. 같은 날짜 리포트 요약과 exact-date KRX 주가/거래대금만 사용한다.
  - 26.05.15 추가 반영: `data/rotation_etf_candidates.json`를 추가하고, operator-managed mapping이 있는 경우에만 `candidate_etfs` read-only preview를 표시한다. 현재는 저장 KRX ETF row로 확인한 반도체/IT 예시만 active다.
  - 26.05.15 상태 보정: 종목/ETF preview 기반은 구현됐으므로 미착수 `[ ]`가 아니라 부분 구현 `[△]`로 본다. 아직 `각 1개 최종 선정`은 하지 않으며, 추천/점수/순위가 아닌 `참고` preview로 유지한다.
- [O] 26.05.11 12:05 | 사용자페이지 우측 최상단?근처로해서 검색바 추가 이를 통해 종목검색이 가능하도록 기능 추가
  - 완료: 사용자용 `web-view` 상단에 선택 날짜 기준 종목명/종목코드 검색바가 있고, 검색 결과 선택 시 해당 날짜의 종목 상세로 이동한다. 전 기간/교차 날짜 검색은 별도 후속 항목으로 둔다.

- [△] 26.05.14 | 장마감 16:00 전후 오늘의 시장 분위기 Telegram 브리핑
  - 작성 의도: 아침 전일자 리포트 브리핑과 별개로, 장마감 전후에 당일 리포트 집중 업종/종목, KRX 지수/거래대금, 가능한 수급 참고를 압축해 보내는 러프 브리핑.
  - 기준: 추천/점수/전략 제안 금지. `시장 분위기`, `눈에 띄는 종목`, `수급 참고`, `확인 포인트`까지만 사용.
  - 구현 후보: 먼저 수동 CLI/테스트 발송으로 폰 가독성을 확인하고, 며칠간 쓸만하면 스케줄 후보로 승격한다.
- [ ] 26.05.20 08:00 | https://github.com/colbymchenry/codegraph 북마크 업로드 6
- [ ] 26.05.21 12:31 | 어제 체크 다했는데 오늘 상태가 메롱이었던 이유
- [ ] 26.05.28 12:20 | 부하가 크지 낞다는 가정하에 우선확인 2종?에 대해선 5분단위 실시간 데이터 가져올 궁리
